{
  "name": "assexp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --port 8888",
    "deploy": "wrangler pages deploy . --project-name mori-team-app",
    "deploy:worker": "wrangler deploy",
    "login": "wrangler login",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
